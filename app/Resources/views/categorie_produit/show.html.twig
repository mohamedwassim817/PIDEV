{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorie_produit</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_Produit.id }}</td>
            </tr>
            <tr>
                <th>Nomcategorie</th>
                <td>{{ categorie_Produit.nomCategorie }}</td>
            </tr>
            <tr>
                <th>Typecategorie</th>
                <td>{{ categorie_Produit.typeCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorie_produit_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categorie_produit_edit', { 'id': categorie_Produit.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
