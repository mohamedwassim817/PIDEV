{% extends 'base.html.twig' %}

{% block body %}
    <h1>Sous_categorie</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_categorie.id }}</td>
            </tr>
            <tr>
                <th>Nomsouscategorie</th>
                <td>{{ sous_categorie.nomSousCategorie }}</td>
            </tr>
            <tr>
                <th>Typesouscategorie</th>
                <td>{{ sous_categorie.typeSousCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('sous_categorie_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('sous_categorie_edit', { 'id': sous_categorie.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
